module.exports.handler = async (event) => {
    console.log(event);
        // validate face
      
        // if not valid
        // return {
        //   status: 'ERROR'
        // };
      
        return {
          status: 'SUCCESS'
        };
};