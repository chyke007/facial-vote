module.exports.handler = async (event) => {
    console.log(event);
        // index face
        // return index and s3 paramters to next step
        return {
            value: result,
            status: 'SUCCESS'
          };
};